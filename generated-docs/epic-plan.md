# Epic Plan — Contact Form

Every epic in this project, what it delivers, and what it builds on. Live status
(not started / in flight / done) is shown by `/status` and the dashboard.

> Plan only — edited during planning on `main`, never on an epic branch.

## Epics

| # | Epic | Delivers | Builds on |
|---|---|---|---|
| 1 | Contact Form (`contact-form`) | A public visitor fills in name, email, and message and sees a "Thanks, we'll be in touch" confirmation on the page after submitting. | — |

## Coverage

Everything in the spec is assigned to an epic:

| What you asked for | Epic |
|---|---|
| Contact form with Name, Email, and Message fields (R1) | Contact Form (`contact-form`) |
| All fields are required (R2) | Contact Form (`contact-form`) |
| Email must be a valid format (R3) | Contact Form (`contact-form`) |
| On-page thank-you confirmation after a valid submit (R4) | Contact Form (`contact-form`) |

_4 requirements, all assigned._
